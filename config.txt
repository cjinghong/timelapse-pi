[Capture Options]
duration = 60              # Duration of timelapse -- in minutes.
interval_per_capture = 1    # Interval per capture -- in seconds. Default 5 seconds/frame

[Camera Options]
resolution = 1024x768       # Resolution of camera.

[Others]
save_to_googledrive = true
autoloop = true             # Setting this to true will generate a new timelapse video every {duration} indefinately